import {ReportForm} from '@/components/ReportForm'; export default async function Page({searchParams}:{searchParams:Promise<{game?:string}>}){const q=await searchParams;return <div className="pageShell narrow"><div className="pageTitle"><div className="eyebrow">REPORT</div><h1>Report a game</h1><p>Tell us about loading problems, incorrect content, or a rights issue.</p></div><ReportForm gameSlug={q.game||''}/></div>}
