import { NextResponse } from 'next/server'; import { sbWrite } from '@/lib/supabase-rest';
export async function POST(req:Request){try{const b=await req.json();const rows=await sbWrite<any[]>('categories','POST',{name:b.name,slug:b.slug,description:b.description||`Play free ${String(b.name).toLowerCase()} games instantly in your browser.`,enabled:b.enabled??true,sort_order:b.sortOrder??0});const r=rows[0]||rows;return NextResponse.json({id:r.id,name:r.name,slug:r.slug,description:r.description,enabled:r.enabled,sortOrder:r.sort_order})}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
