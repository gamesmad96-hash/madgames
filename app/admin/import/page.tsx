import {AdminImportClient} from '@/components/AdminImportClient'; export default function ImportPage(){return <div className="pageShell"><div className="pageTitle"><div className="eyebrow">PROVIDER IMPORT</div><h1>Import games</h1><p>Paste the official JSON/XML feed URL from GameMonetize, GameDistribution, or another licensed provider. Preview first, then publish only the games you want.</p></div><AdminImportClient/></div>}
