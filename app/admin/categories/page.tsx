import { getCategories } from '@/lib/catalog'; import { AdminCategoriesClient } from '@/components/AdminCategoriesClient'; export default async function Categories(){const cats=await getCategories();return <div className="pageShell"><div className="pageTitle"><div className="eyebrow">TAXONOMY</div><h1>Categories</h1><p>Manage the game categories used across MADGAMES.FUN.</p></div><AdminCategoriesClient initial={cats}/></div>}
