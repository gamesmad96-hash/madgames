import Link from 'next/link'; import {getAllGamesForAdmin} from '@/lib/catalog'; export default async function AdminGames(){const games=await getAllGamesForAdmin(500);return <div className="pageShell"><div className="sectionHead"><div className="pageTitle"><div className="eyebrow">LIBRARY</div><h1>Games</h1></div><div className="gameActions"><Link className="secondaryBtn" href="/admin/import">Import</Link><Link className="primaryBtn" href="/admin/games/new">+ Add game</Link></div></div><section className="adminPanel"><div className="adminTable">{games.map(g=><Link href={`/admin/games/${g.id}`} className="adminRow" key={g.id}><span className={`miniArt ${g.gradient}`}>{g.emoji}</span><div><strong>{g.title}</strong><small>{g.category} • {g.provider}</small></div><span className="statusPill">{g.status}</span><span>→</span></Link>)}</div></section></div>}
