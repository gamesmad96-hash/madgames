export default async function AdminLogin({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <div className="loginWrap"><form className="loginCard" action="/api/admin/login" method="post"><div className="eyebrow">MADGAMES CONTROL ROOM</div><h1>Admin login</h1><p>Use the password stored in <code>ADMIN_PASSWORD</code>.</p><input type="password" name="password" placeholder="Admin password" required autoFocus/>{q.error&&<span className="errorText">Wrong password.</span>}<button className="primaryBtn" type="submit">Sign in</button></form></div>}
