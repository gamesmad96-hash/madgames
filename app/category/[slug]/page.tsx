import type {Metadata} from 'next'; import {notFound} from 'next/navigation'; import {getByCategory} from '@/lib/catalog'; import {GameCard} from '@/components/GameCard';
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const {category}=await getByCategory(slug);return category?{title:`${category.name} Games`,description:category.description||`Play free ${category.name.toLowerCase()} games.`}:{};}
export default async function CategoryPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const {category,games}=await getByCategory(slug);if(!category)notFound();return <div className="pageShell"><div className="pageTitle"><div className="eyebrow">CATEGORY</div><h1>{category.name} Games</h1><p>{category.description}</p></div><div className="gameGrid">{games.map(g=><GameCard key={g.id} game={g}/>)}</div></div>}
